package day41;

public class Student {
	public String name;
	protected String course;
	double grade;
	private int age;
	
	//Access modifiers
	//public - accessible from everywhere (properties, methods, and class)
	//protected - accessible within the same package and subclass
	//default - accessible within the same package only (we don't put any access modifiers)
	//private - accessible within the same class	
}
